## Process this file with automake to produce Makefile.in
info_TEXINFOS = clp.texinfo

SUBDIRS = . src

# Workaround for autoconf < 2.60
docdir = ${datadir}/doc/${PACKAGE} 
#end workaround

dist_doc_DATA = README

